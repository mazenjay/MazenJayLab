/** Matches Go api/handlers ArticlePagination page size (pageSize = 5) */
export const ARTICLES_PAGE_SIZE = 5;

/** Spotlight search: single page, max 20 hits (matches API default & cap). */
export const SEARCH_PER_PAGE = 20;
export const SEARCH_API_MAX_PER_PAGE = 20;
